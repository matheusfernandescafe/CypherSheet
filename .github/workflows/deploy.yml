name: Deploy Blazor PWA to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - run: dotnet restore

      - run: dotnet test --configuration Release

      - name: Publish (main only)
        if: github.ref == 'refs/heads/main'
        run: dotnet publish src/CypherSheet.Client/CypherSheet.Client.csproj -c Release -o dist

      - name: Configure GitHub Pages paths
        if: github.ref == 'refs/heads/main'
        run: |
          REPO_NAME=${GITHUB_REPOSITORY#*/}
          sed -i "s|<base href=.*>|<base href=\"/$REPO_NAME/\" />|g" dist/wwwroot/index.html

          if [ -f dist/wwwroot/manifest.webmanifest ]; then
            sed -i "s|\"start_url\": \"/\"|\"start_url\": \"/$REPO_NAME/\"|g" dist/wwwroot/manifest.webmanifest
          fi

          cp dist/wwwroot/index.html dist/wwwroot/404.html
          touch dist/wwwroot/.nojekyll

      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/wwwroot

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
